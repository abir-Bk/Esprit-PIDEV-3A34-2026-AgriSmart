<!DOCTYPE html>
<html lang="fr">
<head>
	<title>{% block title %}AgriSmart{% endblock %}</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

	{# Favicon (logo in browser tab) #}
	<link rel="icon" href="{{ asset('assets/images/logo.png') }}" type="image/svg+xml">
	<link rel="alternate icon" href="{{ asset('assets/images/logo.png') }}" type="image/svg+xml">

	<script>
		// Force le thème clair (white theme) partout
		document.documentElement.setAttribute('data-bs-theme', 'light');
	</script>

	{% block stylesheets %}
		<link rel="stylesheet" type="text/css" href="{{ asset('assets/vendor/font-awesome/css/all.min.css') }}">
		<link rel="stylesheet" type="text/css" href="{{ asset('assets/vendor/bootstrap-icons/bootstrap-icons.css') }}">
		<link rel="stylesheet" type="text/css" href="{{ asset('assets/vendor/overlay-scrollbar/css/overlayscrollbars.min.css') }}">
		<link rel="stylesheet" type="text/css" href="{{ asset('assets/css/style.css') }}">
	{% endblock %}
</head>

<body class="d-flex flex-column min-vh-100">

    {% block header %}
        {{ include('_partials/_header.html.twig') }}
    {% endblock %}

    {# Messages flash globaux (dismissible) — une page peut surcharger ce bloc pour afficher les flashes ailleurs #}
    {% block flash_messages %}
    {% if app.flashes|length > 0 %}
    <div class="container py-2 py-md-3" id="flash-container" role="region" aria-live="polite">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                {% set alert_class = label == 'danger' ? 'danger' : (label == 'warning' ? 'warning' : (label == 'info' ? 'info' : 'success')) %}
                <div class="alert alert-{{ alert_class }} alert-dismissible fade show rounded-3 shadow-sm mb-2 mb-md-3 d-flex align-items-center flash-message" role="alert">
                    <span class="flex-grow-1">{{ message }}</span>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    {% endif %}
    {% endblock %}

    <main class="flex-grow-1">
        {% block body %}{% endblock %}
    </main>

    {% block footer %}
    <footer class="bg-light border-top py-4 mt-auto">
        <div class="container text-center text-muted small">
            <span>© {{ 'now'|date('Y') }} AgriSmart · Marketplace agricole</span>
        </div>
    </footer>
    {% endblock %}

    {% block javascripts %}
        <script src="{{ asset('assets/vendor/bootstrap/dist/js/bootstrap.bundle.min.js') }}"></script>
    {% endblock %}
</body>
</html>
