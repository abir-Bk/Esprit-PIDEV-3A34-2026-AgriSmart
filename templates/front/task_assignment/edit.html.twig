{% extends 'front/base.html.twig' %}

{% block title %}Modifier l'affectation - AgriSmart Admin{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Modifier l'affectation</h5>
            <a href="{{ path('admin_task_assignments_index') }}" class="btn btn-sm btn-outline-secondary">Retour à la liste</a>
        </div>
        <div class="card-body">
            {% include 'front/task_assignment/_form.html.twig' with {form: form, button_label: 'Mettre à jour'} %}
        </div>
    </div>
{% endblock %}

