<header class="navbar-light header-sticky">
  <nav class="navbar navbar-expand-xl">
    <div class="container">

      <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_produit_index') }}">
        <img src="{{ asset('assets/images/logo.png') }}" alt="AgriSmart" style="height:32px;">
        <span class="fw-bold">AgriSmart</span>
      </a>

      <button class="navbar-toggler ms-auto mx-3 p-0 p-sm-2" type="button"
              data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
              aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-animation">
          <span></span><span></span><span></span>
        </span>
      </button>

      <div class="navbar-collapse collapse" id="navbarCollapse">
        <ul class="navbar-nav navbar-nav-scroll mx-auto align-items-xl-center gap-xl-3">
          <li class="nav-item">
            <a class="nav-link {% if app.request.attributes.get('_route') starts with 'app_produit_' %}active{% endif %}"
               href="{{ path('app_produit_index') }}">Marketplace</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="#">Promotions</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="#">Mes commandes</a>
          </li>
        </ul>
      </div>

      {# Actions à droite #}
      <ul class="nav flex-row align-items-center list-unstyled ms-xl-auto gap-3 mb-0">

        {# ✅ PLUS DE SEARCH ICI #}

        <li class="nav-item">
          <a class="btn btn-link text-decoration-none d-flex align-items-center gap-2"
             href="{{ path('app_produit_new') }}">
            <span class="d-inline-flex align-items-center justify-content-center"
                  style="width:22px;height:22px;border-radius:50%;border:1px solid #1f3b2c;">
              +
            </span>
            <span class="fw-semibold">Nouvelle annonce</span>
          </a>
        </li>

        <li class="nav-item">
          <a class="btn btn-outline-secondary btn-sm" href="#">Se connecter</a>
        </li>
      </ul>

    </div>
  </nav>
</header>
